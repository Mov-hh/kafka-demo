Spring Boot + Spring Kafka使用示例02
====================================

测试消息发送

```
curl 'http://localhost:8080/send?count=10&prefix=haha'
```

其中，`count`参数表示发送的消息条数，`prefix`参数表示要发送消息内容的前缀

